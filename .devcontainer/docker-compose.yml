version: '3.8' # Update this to the name of the service you want to work with in your docker-compose.yml file
services:
  client:
    volumes:
      - .:/workspace:cached
    command: /bin/sh -c "while sleep 1000; do :; done"
  server:
    volumes:
      - .:/workspace:cached
    command: /bin/sh -c "while sleep 1000; do :; done"
